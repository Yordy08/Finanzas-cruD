"use strict";Object.defineProperty(exports, "__esModule", { value: true });exports.default = void 0;var _nodeFs = await jitiImport("node:fs");
var _nodePath = await jitiImport("node:path");
var _vitePluginVuetify = _interopRequireDefault(await jitiImport("vite-plugin-vuetify"));function _interopRequireDefault(e) {return e && e.__esModule ? e : { default: e };}var _default = exports.default =

defineNuxtConfig({
  devtools: {
    enabled: true
  },

  modules: ['@pinia/nuxt', '@vite-pwa/nuxt'],

  css: ['vuetify/styles'],

  build: {
    transpile: ['vuetify']
  },

  vite: {
    plugins: [(0, _vitePluginVuetify.default)()]
  },

  runtimeConfig: {
    databaseUrl:
    process.env.DATABASE_URL ||
    process.env.DOTENV_DATABASE_URL ||
    process.env.NUXT_PUBLIC_DATABASE_URL,

    public: {
      appName: 'Finanzas CRUD',
      version: (() => {
        try {
          const pkg = JSON.parse(
            (0, _nodeFs.readFileSync)((0, _nodePath.join)(process.cwd(), 'package.json'), 'utf-8')
          );
          return pkg?.version || '0.0.0';
        } catch {
          return '0.0.0';
        }
      })(),

      MVP_USER_ID:
      process.env.NUXT_PUBLIC_MVP_USER_ID ||
      process.env.MVP_USER_ID ||
      '6a1b3bfed5ae05d6649ac2fc'
    }
  },

  compatibilityDate: '2025-01-01',

  pwa: {
    registerType: 'autoUpdate',
    strategies: 'generateSW',

    devOptions: {
      enabled: true
    },

    manifest: {
      name: 'Finanzas',
      short_name: 'Finanzas',
      description: 'App financiera PWA',
      theme_color: '#0b1220',
      background_color: '#0b1220',
      display: 'standalone',
      scope: '/',
      start_url: '/',

      icons: [
      {
        src: '/logo.png',
        sizes: '64x64',
        type: 'image/png',
        purpose: 'any'
      },
      {
        src: '/logo.png',
        sizes: '192x192',
        type: 'image/png',
        purpose: 'any'
      },
      {
        src: '/logo.png',
        sizes: '512x512',
        type: 'image/png',
        purpose: 'any'
      }]

    },

    workbox: {
      navigateFallback: '/',
      globPatterns: ['**/*.{js,css,html,ico,png,svg,webmanifest,json}'],

      runtimeCaching: [
      {
        urlPattern: /^https:\/\/(.*)\/api\//,
        handler: 'NetworkFirst',
        options: {
          cacheName: 'pwa-api',
          networkTimeoutSeconds: 5,
          expiration: {
            maxEntries: 50,
            maxAgeSeconds: 60 * 60
          }
        }
      },
      {
        urlPattern: ({ request }) => request.destination === 'image',
        handler: 'CacheFirst',
        options: {
          cacheName: 'pwa-images',
          expiration: {
            maxEntries: 60,
            maxAgeSeconds: 60 * 24 * 30
          }
        }
      }]

    }
  }
}); /* v9-2d3469df99e2f7a8 */
