"use strict";Object.defineProperty(exports, "__esModule", { value: true });exports.default = void 0;var _vitePluginVuetify = _interopRequireDefault(await jitiImport("vite-plugin-vuetify"));function _interopRequireDefault(e) {return e && e.__esModule ? e : { default: e };}var _default = exports.default =

defineNuxtConfig({
  devtools: {
    enabled: true
  },

  modules: ['@pinia/nuxt', '@vite-pwa/nuxt'],

  css: ['vuetify/styles'],

  build: {
    transpile: ['vuetify']
  },

  vite: {
    plugins: [(0, _vitePluginVuetify.default)()]
  },

  runtimeConfig: {
    databaseUrl: process.env.DATABASE_URL,

    public: {
      appName: 'Finanzas CRUD',
      MVP_USER_ID: process.env.NUXT_PUBLIC_MVP_USER_ID
    }
  },

  compatibilityDate: '2025-01-01',

  pwa: {
    registerType: 'autoUpdate',
    strategies: 'generateSW',

    devOptions: {
      enabled: true
    },

    manifest: {
      name: 'Finanzas',
      short_name: 'Finanzas',
      description: 'App financiera PWA',
      theme_color: '#0b1220',
      background_color: '#0b1220',
      display: 'standalone',
      scope: '/',
      start_url: '/',

      icons: [
      {
        src: '/logo.png',
        sizes: '64x64',
        type: 'image/png',
        purpose: 'any'
      },
      {
        src: '/logo.png',
        sizes: '192x192',
        type: 'image/png',
        purpose: 'any'
      },
      {
        src: '/logo.png',
        sizes: '512x512',
        type: 'image/png',
        purpose: 'any'
      }]

    },

    workbox: {
      navigateFallback: '/',
      globPatterns: ['**/*.{js,css,html,ico,png,svg,webmanifest,json}'],

      runtimeCaching: [
      {
        urlPattern: /^https:\/\/(.*)\/api\//,
        handler: 'NetworkFirst',
        options: {
          cacheName: 'pwa-api',
          networkTimeoutSeconds: 5,
          expiration: {
            maxEntries: 50,
            maxAgeSeconds: 60 * 60
          }
        }
      },
      {
        urlPattern: ({ request }) => request.destination === 'image',
        handler: 'CacheFirst',
        options: {
          cacheName: 'pwa-images',
          expiration: {
            maxEntries: 60,
            maxAgeSeconds: 60 * 24 * 30
          }
        }
      }]

    }
  }
}); /* v9-26304e9d069d9e28 */
